# lazycat-app-publisher 同步与 Darwin 验证

## 来源范围

- 来源仓库：`/home/czyt/code/others/lzc-developer-doc`。
- 核对窗口：2026-07-09 至 2026-09-09，本地 HEAD `44b77705ac1d734fbe59fcdd326498c73a256672`。
- 仅读取本地 Git 历史和文件；没有修改来源仓库，也没有声称抓取了远端最新版本。

| 提交 | 内容 | 同步处理 |
|------|------|----------|
| `73f660e` | 废弃 application.handlers | 已有 injects 替代指引，保留并回归 |
| `8a64d7d` | L4 转发动态状态 | 已有端口和 2 字节前缀规则；补充虚拟 IP、规则加载和匹配过程 |
| `6420cc0` / `1888200` | 原创激励 100–500、无激励分类 | 现有 store-rule 已覆盖；补充激励、上架、自用适用范围 |
| `1d8b897` | fuse.mount | 已有 helper 权限和版本契约，保留 |
| `688597f` | 物理显示器 VT | 已有权限、runtime、状态码和验证步骤；回归发现并补齐 activate 在实际激活后成功返回的语义 |
| `955f9ff` | 算力舱文档导航 | 仅站点外部入口，不产生本 skill 的新配置规则 |
| `14c32b4` | inject ctx 合并与匹配说明 | 补充严格前缀边界、ctx.client.id 与开发机状态来源；既有 API 表不重复复制 |
| `0953c7b` | 动态应用图标 | 新增 dynamic-icon.md，并以正式版本记录明确 1.6.2 门槛 |
| `238415c` | v1.6.1 发布记录 | 补充取消默认 /dev/fuse 挂载的迁移要求 |
| `cf4761e` / `44b7770` | exec 健康监测、v1.6.2 正式发布 | 解释旧路由遗漏及端口不一致的升级故障，使用正式发布日期 2026-09-09 |
| `ca59e19` | unsupported_platforms | 新增客户端平台表、v1/v2 文件位置与 CPU 架构区分 |

`docker.sock` 调整是用户明确指定的维护策略：社区 / 自用按需求保留挂载，核实目标 daemon、路径和权限；LightOS 是推荐选择。不能将上游推荐解释为全面禁令，也不宣称官方保证支持或商店批准。修正范围包含主文档、architecture、strict-constraints、spec、advanced-features、quick-reference、智能分析伪代码及 Portainer/Jenkins 示例。

## Writing Skills RED → GREEN

修改前分别运行无 skill 对照、旧 skill 检索测试。以下摘录为独立 agent 的实际输出，不是构造的失败结果。

| 场景 | 无 skill 对照 | 旧 skill（RED） | 新 skill（GREEN） |
|------|---------------|----------------|------------------|
| 社区自用 socket | “可以继续转换” | “停止生成 LPK，迁移到 LightOS，不生成宿主 socket 挂载。” | 保留 compose_override 挂载，检查路径/UID/GID/API，不要求官方批准 |
| 按用户动态图标 | 无法可靠给出版本和路径 | “当前资料仅说明构建时 icon: ./icon.png” | 1.6.2、真实 UID 文件、三级优先级、PNG ≤1 MiB、4 FPS、重启再生成 |
| exec 升级后不健康 | 能识别端口不一致，无法确认版本原因 | “当前资料不足以解释为什么升级 1.6.2 才出现问题。” | 明确 v1.6.2 自动健康监测修复，统一声明与监听端口 |

新加 test-prompts ID24–29 的实际检索 / 作答测试 **6/6 PASS**，覆盖 socket、动态图标、exec、客户端平台、FUSE、inject 匹配与客户端身份。24 种动态图标内容被正确解释为缓存建议，不是硬限制；`:ro` 未被误解为 Docker API 只读。

## Darwin 优化

同步后保留主文档快照，进行一轮小幅优化：前置任务 / 症状参考入口、补齐触发词和版本表，并移除社区自用 compose_override 的无条件备案门槛。三名独立 judge 同次对比优化前后主文档，以九维 rubric 比较；不以不同 judge 的绝对分差决定保留。

独立 triage 原始维度分为 `[8,8,8,6,9,8,7,10,8]`，权重 `[7,12,12,6,18,4,12,23,6]`，加权 **84/100**。这是单次质量粗评，维度 8 仅基于上述六项场景，不能当作全部场景或真实设备通过率，也没有可比的优化前后绝对分差。

剩余限制：主文档仍较长，保留了本次范围之外的旧检查点；本轮不宣称完全消除了冗余或重复确认。

## 验证边界

场景测试是独立 agent 的检索与应用测试，未在真实微服部署 LPK，未操作 Docker daemon。格式校验包括 skill frontmatter、29 个唯一测试 ID、改动文档相对链接、git diff 空白检查，以及未修改发布脚本的 bash 语法检查。

## Paired 结论与回归记录

三位独立 judge：**3–0 better**，margin 分别为 clear、slight、clear。共同依据是消除自用备案冲突并改善参考导航；未把新增字数或绝对分上涨当成收益。保留一轮优化，无回滚。

旧能力回归 ID10、13、14、15、17、18、21、23 首轮为 7/8：ID15 缺少 activate 成功返回时机，已由来源 `docs/advanced-vt.md` 确认并补齐。其余覆盖多镜像选择、数据目录、L4 前缀、模板、handlers 废弃和包名稳定性。

ID15 修复后独立复测 PASS，旧能力回归最终 **8/8 PASS**；合计 14 个已执行场景通过（测试集共 29 项，未声称全量执行）。HTML 成果卡已生成；PNG 截图脚本因缺少 playwright/playwright-core 未能运行。
